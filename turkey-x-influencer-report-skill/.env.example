# 抓取（OpenRouter → xAI x_search，走 /responses）
# 申请：https://openrouter.ai/keys
# 填 sk-or-v1-... 即可；环境变量名固定为 XAI_API_KEY（与 config 中 api_key_env 一致）
XAI_API_KEY=sk-or-v1-你的OpenRouter密钥

# 翻译 + 精选分析（默认 MiniMax；可按 config.json 的 llm 段改）
MINIMAX_API_KEY=你的MiniMax密钥
